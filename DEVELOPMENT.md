# 开发指南 🚀

> 本指南适用于 Capstone Project - 全栈应用开发

---

## 🏁 首次设置（只需执行一次）

### 后端环境配置
```bash
cd backend_py
# 安装依赖
pip install -r requirements.txt

# 确认所有工具都安装好了
black --version
pylint --version
pytest --version
tox --version
```

### 前端环境配置
```bash
cd frontend

# 安装依赖
npm install

# 安装开发工具（如果还没安装）
cd frontend
npm install --save-dev eslint prettier eslint-config-prettier eslint-plugin-react
```

---

## 📋 日常开发流程

### 后端开发 (backend_py)

```bash
cd backend_py

# 1. 格式化代码
black .
# ✅ 预期输出: "reformatted main.py" 或 "All done! ✨"

# 2. 检查代码规范
pylint main.py
# ✅ 预期输出: "Your code has been rated at 8.5/10" (需要 > 8.0)

# 3. 运行测试
pytest tests/ -v
# ✅ 预期输出: "===== X passed in X.XXs ====="

# 4. 运行完整的 tox 测试（可选，提交前建议运行）
tox
```

### 前端开发 (frontend)

```bash
cd frontend

# 1. 格式化代码
npm run format
# ✅ 预期输出: 显示格式化的文件列表

# 2. 检查代码规范
npm run lint
# ✅ 预期输出: 无错误信息

# 3. 如果有 ESLint 错误，尝试自动修复
npm run lint:fix

# 4. 构建项目
npm run build
# ✅ 预期输出: "built in XXXms"
```

---

## ✅ 提交代码前的检查清单

**每次提交代码前，务必按顺序执行：**

### 后端检查
- [ ] `black .` - 格式化代码 ✨
- [ ] `pylint main.py` - 通过代码检查（评分 ≥ 8.0）
- [ ] `pytest tests/` - 所有测试通过 ✅
- [ ] `tox` - 完整测试通过（重要提交时）

### 前端检查
- [ ] `npm run format` - 格式化代码 ✨
- [ ] `npm run lint` - 无 ESLint 错误 ✅
- [ ] `npm run build` - 成功构建 📦

### 如果所有检查都通过 ✅
```bash
# 查看修改的文件
git status

# 添加修改
git add .

# 提交（写清楚改了什么）
git commit -m "feat: 添加用户登录功能"
# 或
git commit -m "fix: 修复数据加载bug"

# 推送到 GitHub
git push origin main
```

---

## Git 提交信息规范

使用清晰的提交信息：

```bash
# 功能开发
git commit -m "feat: 添加XX功能"

# Bug修复
git commit -m "fix: 修复XX问题"

# 代码重构
git commit -m "refactor: 重构XX模块"

# 文档更新
git commit -m "docs: 更新README"

# 样式调整
git commit -m "style: 调整按钮样式"
```

---

## GitHub Actions 自动化测试

### 查看测试结果

1. 推送代码后，访问：`https://github.com/你的用户名/仓库名/actions`
2. 点击最新的工作流运行记录
3. 查看 **Backend Tests** 和 **Frontend Tests** 两个任务

### ✅ 成功的标志
- 绿色的 ✓ 标记
- 所有步骤都通过

### ❌ 失败时怎么办

**步骤 1：查看错误日志**
- 点击失败的任务（红色 ✗）
- 展开失败的步骤查看详细错误

**步骤 2：在本地复现**
```bash
# 如果是后端测试失败
cd backend_py
pytest tests/ -v

# 如果是前端测试失败
cd frontend
npm run lint
npm run build
```

**步骤 3：修复后重新提交**
```bash
# 修复代码后
git add .
git commit -m "fix: 修复CI测试错误"
git push
```

---

## ⚠️ 重要注意事项

1. **永远不要跳过本地测试** 
   - ❌ 不要直接 push 没测试过的代码
   - ✅ 本地测试通过后再 push

2. **代码格式化是强制性的**
   - Black 和 Prettier 会自动修改你的代码
   - 格式化后记得再检查一遍代码

3. **处理 merge 冲突**
   ```bash
   # 拉取最新代码
   git pull origin main
   
   # 如果有冲突，手动解决后
   git add .
   git commit -m "merge: 解决冲突"
   ```

## 常见问题排查

### ❌ Black 格式化失败
```bash
cd backend_py
black . --verbose --check
# 查看哪些文件需要格式化

# 强制格式化所有文件
black . --verbose
```

### ❌ Pylint 评分过低（< 8.0）
```bash
# 查看详细警告
pylint main.py --verbose

# 常见问题：
# - C0111: 缺少文档字符串 → 添加函数说明
# - C0103: 变量命名不规范 → 使用 snake_case
# - R0913: 参数过多 → 重构函数
```

**快速提高评分的方法：**
- 添加函数文档字符串
- 删除未使用的导入和变量
- 遵循 PEP 8 命名规范

### ❌ ESLint 错误
```bash
# 查看所有错误
npm run lint

# 自动修复（大部分问题）
npm run lint:fix

# 如果修复不了，手动修改代码
```

**常见 ESLint 错误：**
- `no-unused-vars`: 删除未使用的变量或加 `_` 前缀
- `no-console`: 删除 `console.log()` 或改用日志库

### ❌ Pytest 测试失败
```bash
# 查看详细错误
pytest tests/ -v -s

# 只运行某个测试文件
pytest tests/test_main.py -v

# 显示 print 输出（调试用）
pytest tests/ -v -s
```

### ❌ npm run build 失败
```bash
# 清除缓存重新安装
rm -rf node_modules package-lock.json
npm install

# 检查是否有 ESLint 错误
npm run lint
```

### ❌ GitHub Actions 一直失败
1. 确保本地所有测试都通过
2. 检查 `.github/workflows/ci.yml` 配置是否正确
3. 查看 Actions 日志找到具体错误
4. 在本地运行相同的命令

---

